  <div class="header">
    <h1>Lista de Clientes
    </h1>
  </div>

  <table class="client">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Telefone</th>
        <th>Endereço</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @clients.each do |client| %>
        <tr>
          <td><%= client.name %></td>
          <td><%= client.phone_number %></td>
          <td><%= client.address %></td>
          <%# <td><%= link_to 'Mostrar', client %><%#</td> %>
          <td><%= link_to 'Editar', edit_client_path(client) %></td>
          <td><%= link_to 'Deletar', client, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
  <h1 class="footer">
    <%= link_to 'Novo Cliente', new_client_path %>
    <%= link_to " Novo pedido", new_order_path %>
    <%= link_to " Ver todos os pedidos",  orders_path %> 
  </h1>