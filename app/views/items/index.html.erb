<h1>Lista de itens</h1>

<table>
  <tr>
    <th>Tipo</th>
    <th>Preçp</th>
    <th>Cor</th>
    <th>Cliente</th>
    <th>Pedido</th>
    <th>Item</th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.tipo %></td>
    <td>R$ <%= item.price %></td>
    <td><%= item.color %></td>
    <td><%= item.order.client.name %></td>
    <td><%= item.order_id %></td>
    <td><%= item.id %></td>
    <td><%= link_to 'Mostrar', [item.order, item] %></td>
    <td><%= link_to 'Editar', edit_order_item_path(item.order, item) %></td>
    <td><%= link_to 'Deletar', [item.order, item], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Item', new_order_item_path(params[:order_id]) %>
<%= link_to 'Voltar', orders_path %>

